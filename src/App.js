import './App.css';
import { Card } from './components/card/card';
import { baseUrl, initialUrl } from './services/poke-api/poke-api'

function App() {
  return (
    <div>
      <Card>
      </Card>
    </div>
  );
}

export default App;
