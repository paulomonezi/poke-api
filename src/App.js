import './App.css';
import { Card } from './components/card';
import { baseUrl, initialUrl } from './poke-api/poke-api'

function App() {
  return (
    <div>
      <Card>
      </Card>
    </div>
  );
}

export default App;
